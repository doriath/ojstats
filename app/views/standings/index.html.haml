%h1 Ranking
%table.table.table-striped
  %thead
    %tr
      %th #
      %th User
      %th Judges
      %th Score
  %tbody
    = render 'navigation'

    - @ranking.positions.each_with_index do |position, index|
      %tr
        %td= index + 1
        %td= link_to position[:user_name], user_path(position[:user].id)
        %td
          - position[:judges_points].each do |judge|
            %span{class: ['badge', judge[:name]], title: judge[:name]}= judge[:points]
        %td= position[:score]
